name: Build wheels

on:
  release:
    types: [created]
  push:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        echo "pwd"
        pwd
        sudo apt-get install tree
        tree ./
        tree /home/runner/work
    - name: Python wheels manylinux build
      uses: RalfG/python-wheels-manylinux-build@13e5075
